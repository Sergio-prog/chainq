# Implementation Plans

Generated by the improve skill (direction audit) on 2026-07-07 at commit
`4b48de2`; extended on 2026-07-10 at commit `7b4fb6f` with the selected
Robinhood Chain, Kamino, and Pump/PumpSwap plans. Execute in the order below
unless dependencies say otherwise. Each executor: read the plan fully before
starting, honor its STOP conditions, and update your row when done.

Repo-wide rules that bind every plan: `uv run ruff check .` and
`uv run pytest -q` must pass before finishing; live-test changed commands
against real endpoints; no code comments; never commit/push — leave work
on the named branch uncommitted and report to the owner.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 007 | Add Robinhood Chain mainnet | P1 | S | — | TODO |
| 008 | Add Kamino lending markets | P1 | M | — | TODO |
| 001 | Accept SPL mints in `price`/`asset` | P1 | S | — | TODO |
| 009 | Add read-only Pump/PumpSwap state | P1 | L | 001 | TODO |
| 003 | `chainq yields` — cross-protocol yield comparison | P1 | M | 008 | TODO |
| 002 | `chainq read` — generic contract read | P1 | M | — | TODO |
| 004 | `chainq tx` decodes function + token transfers | P2 | M | — | TODO |
| 005 | `uniswap quote` — amount-aware swap quotes | P2 | M | — | TODO |
| 006 | Polymarket under `protocols` | P3 | M | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 009 depends on 001 because both touch Solana-mint routing in
  `chainq/commands/market.py`; land 001 first, then extract the generic
  DexScreener provider from the reconciled code.
- 003 depends on 008 so the cross-protocol yield command includes Kamino supply
  APYs on first release instead of adding another provider immediately after.
- 007 and 008 are isolated and can execute in parallel worktrees. 002 and 005
  add ABI-adjacent code to different files and can also run independently.
- Every plan updates `skills/chainq/SKILL.md`; when batching features, merge by
  section and regenerate `site/public/llms-full.txt` after the final skill edit.
- Version bumps: each plan is a user-visible feature (minor bump per repo rule); when several land in one release, bump once — the owner decides the release cut.

## Verification baseline

- At `7b4fb6f`, `uv run ruff check .` passes.
- The unit suite passes 61 tests with a normal terminal environment. In hosts
  that export both `TERM=dumb` and `NO_COLOR`, three existing color assertions
  fail because they clear `NO_COLOR` but do not replace `TERM`; use
  `env -u NO_COLOR TERM=xterm uv run pytest -q` only to distinguish that
  environment issue from new failures. Do not waive unrelated failures.

## Findings considered and rejected

- **Watch/stream mode + threshold alerts**: a daemon changes the tool's character and agents can loop the CLI themselves; stays on ROADMAP "Later". Not planned.
- **MCP wrapper**: deliberately parked pending demand from non-shell agents (ROADMAP "Later"); the skill-first thesis is working. Not planned.
- **CEX prices via ccxt**: heavy dependency for a cross-check CoinGecko mostly covers. Not planned.
- **Robinhood Chain testnet and stock-token registry**: mainnet support is the
  useful zero-setup primitive; testnet does not match the production registry,
  and token addresses need separate official validation. Not included in 007.
- **Kamino's entire product suite in one integration**: liquidity strategies,
  KVaults, leverage, transactions, and raw user obligations have different
  schemas and accounting risk. Plan 008 deliberately ships lending markets;
  normalized wallet positions are the recommended follow-up.
- **Pump trading and transaction builders**: signing, slippage, fee routing,
  simulations, and fast-changing account requirements are a materially
  different security surface. Plan 009 remains read-only.
