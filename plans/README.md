# Implementation Plans

Generated by the improve skill (direction audit) on 2026-07-07 at commit
`4b48de2`; extended on 2026-07-10 at commit `7b4fb6f` with the selected
Robinhood Chain, Kamino, and Pump/PumpSwap plans. Reconciled on 2026-07-16
against `main` at `d3e341e`, and again on **2026-07-26 against `main` at
`a18daab`**. Execute in the order below unless dependencies say otherwise. Each
executor: read the plan fully before starting, honor its STOP conditions, and
update its row when done.

Repo-wide rules that bind every plan: `uv run ruff check .` and
`uv run pytest -q` must pass before finishing; live-test changed commands
against real endpoints; no code comments; never commit/push to `main` — leave
work on the named branch and report to the owner.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 008 | Add Kamino lending markets | P1 | M | — | DONE — merged via PR #4; re-verified live 2026-07-26 |
| 001 | Accept SPL mints in `price`/`asset` | P1 | S | — | DONE — merged via PR #5; re-verified live 2026-07-26 |
| 003 | `chainq yields` — cross-protocol yield comparison | P1 | M | 008 | DONE — merged via PR #3; re-verified live 2026-07-26 |
| 004 | `chainq tx` decodes function + token transfers | P2 | M | — | DONE — merged via PR #6 on 2026-07-27 (`ec93976`) |
| 007 | Add Robinhood Chain mainnet | P1 | S | — | IN PROGRESS — branch `b0a46f5` is sound and upstream re-verified, but needs the Step 0 replay onto current `main` (docs conflict, code does not) |
| 009 | Add read-only Pump/PumpSwap state | P1 | L | 001 (merged) | TODO — refreshed to `a18daab`, executable |
| 010 | `chainq tx` names the action — swaps, approvals, lending, vaults, wraps | P1 | L | 004 (merged) | TODO — written 2026-07-28 at `ec93976`; every event signature verified live |
| 005 | `uniswap quote` — amount-aware swap quotes | P2 | M | — | TODO — zero drift since it was written; directly executable |
| 011 | `chainq tx` sees through bundlers and multisigs (ERC-4337, Safe) | P2 | M | **010** | TODO — blocked on 010; event layouts read out of real receipts |
| 006 | Polymarket under `protocols` | P3 | M | — | TODO — refreshed to `a18daab`, executable |
| 002 | `chainq read` — generic contract read | P1 | M | — | REJECTED — v0.16 independently shipped the capability as `chainq evm call` |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 009 depended on 001, which is now **merged into `main`** — start 009 from
  `main`, not from `feat/solana-mints-price`.
- 003 depended on 008; both are merged.
- 007 must not be rebased or merged wholesale — see Step 0 in its plan.
- 004 is merged; 010 builds directly on the `_decode_transfers` helper it added.
- 011 depends on 010 for `chainq/decode.py` and its `(topic0, topic_count)`
  registry key. Do not start 011 first — it has no decoder to extend.
- Every plan updates `skills/chainq/SKILL.md`; when batching features, merge by
  section and regenerate `site/public/llms-full.txt` after the final skill edit
  (`pnpm --dir site build` runs `scripts/gen-llms-full.mjs` as `prebuild`).
- Version bumps: each plan is a user-visible feature (minor bump per repo rule);
  when several land in one release, bump once — the owner decides the release cut.
  Plan 004 is unreleased and needs a bump whenever it is cut.

## Verification baseline

- At `a18daab` on 2026-07-26, `uv run ruff check .` prints `All checks passed!`
  and `uv run pytest -q` reports **130 passed, 24 deselected**. (The older
  71-test figure in the 2026-07-16 notes is superseded.)
- In hosts that export both `TERM=dumb` and `NO_COLOR`, three existing color
  assertions fail because they clear `NO_COLOR` but do not replace `TERM`; use
  `env -u NO_COLOR TERM=xterm uv run pytest -q` only to distinguish that
  environment issue from new failures. Do not waive unrelated failures.

## Reconciliation notes — 2026-07-26

Every claim below was checked against the code or a live endpoint, not inferred
from the previous status table.

- **004 executed and approved.** An executor implemented it in an isolated
  worktree; review took two rounds. Round 1 was returned for two defects: the
  token-metadata `multicall` was unguarded (a transient RPC failure would have
  aborted the whole `tx` command — a core command that worked before), and
  `fourbyte.signature` could still raise on a malformed payload despite the
  plan's never-raise requirement. Both were fixed in round 2. Final state on
  `feat/tx-decode` (`e08d800`): ruff clean, **140 passed, 24 deselected**, scope
  limited to the 5 in-scope files. Independently verified by the reviewer: a
  confirmed plain ETH send produces **byte-identical output** to `main`, a
  confirmed ERC-20 tx renders `calls transfer(address,uint256)` plus
  `25.5943 USDT: 0xaA8b…3efb → 0x6BFA…24F6`, and `--format table|toon|json`
  and `-q` all exit 0.
- **001, 003, 008 re-verified DONE**, not just assumed: `chainq price <JUP mint>`
  and `chainq asset <BONK mint>` resolve; `chainq yields -l 3` returns live Sky
  and Aave rows; `chainq protocols kamino markets` is registered.
- **007 diagnosed precisely.** `git merge-tree --write-tree --name-only main
  feat/robinhood-chain` (read-only) shows `chainq/networks.py`,
  `tests/test_networks.py`, `tests/test_live.py`, and
  `site/public/llms-full.txt` **auto-merge**; `README.md`, `ROADMAP.md`,
  `site/index.html`, `site/public/llms.txt`, and `skills/chainq/SKILL.md`
  conflict because the branch carries v0.15-era summary text that `main` has
  since rewritten for v0.17.1. A plain rebase would silently revert
  documentation, so the plan gained a **Step 0: replay, don't rebase**. Upstream
  re-verified the same day: `eth_chainId` → `0x1237` (= 4663), explorer HTTP 200.
- **005 has zero drift** — `chainq/commands/uniswap.py`,
  `chainq/providers/uniswap_data.py`, and `chainq/rpc.py` are byte-identical to
  the `4b48de2` planning commit, so every line number in the plan still holds.
  `uniswap.py` exposes only `pools`, `pool`, `stats` — no `quote`.
- **006 refreshed** — `chainq/commands/protocols.py` gained `kamino` and `llama`
  mounts since the plan was written, so its code excerpt was stale and has been
  replaced with the current file. No Polymarket module, provider, or mount
  exists anywhere in the tree.
- **009 refreshed** — `chainq/commands/market.py` drifted only by an unrelated
  `--links` option on `asset` (lines ~236-300); the DexScreener routing region
  the plan depends on (`market.py:14-65` and the three Plan-001 helpers) is
  byte-identical, and `chainq/providers/uniswap.py` and `chainq/solana.py` are
  unchanged. No `pump` module exists.
- **Housekeeping done 2026-07-26**: the plan-004 executor worktree was removed
  after confirming its branch was fully pushed and its tree clean, and the four
  stale `prunable` entries under `/private/tmp/chainq-plan-00{1,3,7,8}` were
  pruned. `git worktree list` is now just the main checkout.
- **Branches still outstanding**: `feat/tx-decode` (PR #6, keep until merged) and
  `feat/robinhood-chain` (keep until 007 is replayed) are the only two not
  merged into `main`. `feat/kamino-markets`, `feat/solana-mints-price`,
  `feat/yields-command`, `feat/robinhood-chain-local`, and the leftover harness
  branch `worktree-agent-ad610cbd92282d20b` are all fully merged and safe to
  delete; left in place because deleting branches is the owner's call.
- **Note**: `.claude/` is not gitignored, so the harness's `.claude/worktrees/`
  directory shows up as untracked whenever an executor runs. Worth adding to
  `.gitignore` so it can never be committed by accident.

## Extension notes — 2026-07-28

Plans 010 and 011 were written at `ec93976` (PR #6 merged) as the follow-up to
004. Both were grounded live rather than from memory:

- Every event signature in 010's registry table was confirmed by `eth_getLogs`
  over Ethereum blocks 25627804–25627864, and the table records the observed
  emitter and hit count per row. Four rare signatures (Aave `LiquidationCall`,
  two Compound III events, Morpho Blue `Supply`) produced **zero hits** in the
  window; the plan requires the executor to observe each live or drop it.
- 011's `UserOperationEvent` and Safe `ExecutionSuccess` layouts were read word
  by word out of real receipts, not inferred from an ABI. That is how the
  decisive detail surfaced: `ExecutionSuccess` is emitted with **two different
  topic layouts** by different Safe contract versions under one topic0, which is
  why 010's registry is keyed by `(topic0, topic_count)` rather than topic0
  alone. A topic0-only registry would mis-decode one of the two into confident
  nonsense.
- Two EntryPoint versions (v0.6 and v0.7) were both observed live emitting the
  same `UserOperationEvent` topic0, so 011 detects account abstraction by the
  event and reads the EntryPoint off the emitter — no address list to maintain.
- The "before" output quoted in both plans' *Why this matters* sections is real
  `chainq tx` output captured at `ec93976`, including the Uniswap v4 swap whose
  ETH leg is invisible because v4 settles natively without a `Transfer` log.

## Findings considered and rejected

- **Watch/stream mode + threshold alerts**: a daemon changes the tool's character and agents can loop the CLI themselves; stays on ROADMAP "Later". Not planned.
- **MCP wrapper**: deliberately parked pending demand from non-shell agents (ROADMAP "Later"); the skill-first thesis is working. Not planned.
- **CEX prices via ccxt**: heavy dependency for a cross-check CoinGecko mostly covers. Not planned.
- **Robinhood Chain testnet and stock-token registry**: mainnet support is the
  useful zero-setup primitive; testnet does not match the production registry,
  and token addresses need separate official validation. Not included in 007.
- **Kamino's entire product suite in one integration**: liquidity strategies,
  KVaults, leverage, transactions, and raw user obligations have different
  schemas and accounting risk. Plan 008 deliberately ships lending markets;
  normalized wallet positions are the recommended follow-up.
- **Pump trading and transaction builders**: signing, slippage, fee routing,
  simulations, and fast-changing account requirements are a materially
  different security surface. Plan 009 remains read-only.
- **Top-level `chainq read` command**: v0.16 independently shipped generic
  ABI-less contract reads as `chainq evm call`, backed by shared calldata and
  ABI decoding helpers. A second parser and top-level alias would duplicate the
  same capability without adding material coverage. Plan 002 is rejected.
- **Per-token `erc20()` retry when the `tx` metadata multicall fails** (raised
  during 004 review): `chainq/commands/uniswap.py` does this, but it costs one
  RPC call per token in a command that must stay fast. `tx` degrades silently to
  short addresses and raw amounts instead. Deliberate divergence from that
  exemplar.
- **Pricing swaps from pool internals** (raised while writing 010): decoding
  `amount0`/`amount1` out of a `Swap` event needs the pool's `token0`/`token1`,
  which is a second multicall round-trip *after* the first resolves — two extra
  round-trips on a command that must stay fast. The same amounts are already
  present in the `Transfer` logs of the same transaction, so 010 computes the
  sender's net token flow from those instead, at zero extra RPC cost. Swap
  events are registered for classification only.
- **Full calldata argument decoding for `tx`**: `chainq evm abi-decode` and the
  helpers in `chainq/evm.py` can decode arguments given a signature, and 4byte
  supplies a candidate signature — but 4byte selector collisions are real and
  plan 004's standing rule is that the function name is a label nothing may
  branch on. Decoding arguments against a possibly-wrong signature produces
  confidently wrong output, which is worse than none. Not planned; revisit only
  behind a verified ABI source.
- **Trace-based decoding (`debug_traceTransaction` / `trace_transaction`)**:
  would expose internal calls and native transfers that logs miss (the Uniswap
  v4 ETH leg in 010's example). Rejected because the public RPC pool in
  `chainq/networks.py` does not offer debug namespaces — the feature would be
  unavailable on most networks and would make `tx` behave differently depending
  on which fallback endpoint answered.
- **Rendering NFT transfers with token ids and collection names**: ERC-721/1155
  rows are in 010's registry for classification, but rendering them well needs
  per-collection metadata and a token-id display policy. Deliberately deferred
  to its own plan rather than bolted onto the transfer list.
- **Recursive decoding of inner UserOperation `callData` and Safe multisend
  payloads**: a genuine nested call tree is a different output shape (a tree,
  not a line list) and a materially larger surface. 011 surfaces inner effects
  through the events and transfers they already emit; recursion is a follow-up.
